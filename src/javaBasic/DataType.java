package javaBasic;

public class DataType {
	public static void main(String[] args) { // main method which is required to run java
		// variable is a container which stores the value that can be changed
		// object has state and behavior
		// state--> variable
		// Behavior--> method
		int numberOfWheels = 4;
		boolean isSmoke = false;
		char firstChaName = 'm';
		float earnPerHour = 12.75f;
		double income = 45000.34;
		// Data type is a type of data the variable stores Data type is declaired
		// on the basic of the value the variable is holding
		// byte, short, int, long = whole number
		// float, double = decimal number
		// char =single character
		// boolean = true or false

		System.out.println(" The Numbers of wheels " + numberOfWheels);
        System.out.println(isSmoke);
        System.out.println(firstChaName);
        System.out.println(earnPerHour);
        System.out.println(income);
        
	}

}
